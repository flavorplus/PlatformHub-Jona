name = "enforcepolicy"
violation_action = "block"

conditions {
    rego = <<-EOT
            package enforcepolicy
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package enforcepolicy
            
            default evaluate := true
            EOT
}